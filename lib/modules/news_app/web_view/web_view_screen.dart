/*

import 'package:flutter/material.dart';
import 'package:webview_flutter/webview_flutter.dart';

class WepViewScreen extends StatelessWidget {
  final String url;

  WepViewScreen(this.url);

  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: WebView(
        initialUrl: '',
      ),
    );
  }
}

*/
